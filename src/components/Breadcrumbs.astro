---
const { items = [] } = Astro.props;
---
<nav class="academy-breadcrumbs" aria-label="Migas de pan">
  <ol>{items.map((item, index) => <li>{index < items.length - 1 ? <a href={item.href}>{item.label}</a> : <span aria-current="page">{item.label}</span>}</li>)}</ol>
</nav>
